all:
  children:
    web:
      hosts:
        aws1:
          ansible_host: #<ec-2 ip address>
          ansible_user: #user[eg: ubuntu]
          ansible_ssh_private_key_file: #pem-file or the security key file 
